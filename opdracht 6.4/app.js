function calculate(x, y) {
    return x + y;
}

console.log (calculate(18, 51))

function multiplyByFive (x, y) {
    return x * y;
}

console.log (multiplyByFive (18, 5))

function calculateMinutes(x) {
    return x / 60;
}

console.log (calculateMinutes(3600))
